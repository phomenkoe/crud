# vue-laravel-crud

### Installation

1. Clone repo

2. Change to directory

````
cd crud
````   

3. Copy .env file

```
cp .env.example .env
```

4. Run docker

```
docker-compose -f infrastructure/docker-compose.yml up --build
```